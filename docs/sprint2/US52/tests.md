## TESTS

See `analysis.md` § Test coverage for the authoritative list of JUnit classes.

Run module tests:

```bash
cd aisafe.base/aisafe.core && mvn -q test -Dtest=eapli.aisafe.airportmanagement.**.*
```
